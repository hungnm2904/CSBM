(function() {
    'use strict';

    angular
        .module('app.application', [
            'app.application.classes'
        ])
        .config(config);

    /** @ngInject */
    function config($stateProvider) {

    }
})();
